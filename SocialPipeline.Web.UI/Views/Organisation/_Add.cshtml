@model SocialPipeline.Web.ViewModels.Organisation.AddOrganisationViewModel

@*@using (Ajax.BeginForm(new AjaxOptions { OnSuccess = "addOrganisationCallback", HttpMethod = "POST", UpdateTargetId = "orgDialog" }))
{
    @Html.ValidationSummary(true)
   
    @Html.EditorForModel()
 
    <p>
        <input type="submit" value="Add" />
    </p>
}  *@ 

<div class="edit-box">
    
    <div class="editor-label">
        @Html.LabelFor(m => m.Name)
    </div>
    <div class="editor-field">
        @Html.TextBoxFor(m => m.Name, new { @class = "organisationName"})
        @Html.ValidationMessageFor(m => m.Name)
    </div>
</div>